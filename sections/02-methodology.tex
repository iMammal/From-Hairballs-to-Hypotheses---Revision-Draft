\section{Review Methodology}
\label{sec:methodology}
\draftnote{Morris. Insert exact rules from the frozen protocol and report executed procedures. Do not reconstruct eligibility criteria or completed validation from conversation alone.}

The review examines how computational assistance participates in life-science visual analysis, which tasks it supports, and what evidence establishes its consequences for scientific work. Its scope follows the relationship among the data, the visual representation, the computational contribution, and the scientist’s role. A system’s use of an AI method or an immersive display alone does not establish that relationship.

The review protocol separates eligibility from classification and synthesis. Eligibility determines whether a paper reports a qualifying system. The five-task lens, assistance modes, and visualization environments introduced in Section 3 describe the work supported by eligible systems and provide a basis for comparison. The locus of analytic agency guides interpretation of documented computational interventions and human responsibilities; it is not an additional eligibility criterion or categorical score. This separation allows systems with different technologies and levels of automation to be examined under the same inclusion rules.

The following subsections specify scope and eligibility, sources and retrieval, record reconciliation, screening and human decisions, coding and synthesis, and reproducibility. The eligibility rules below follow the frozen coding rubric, version 1.0.0, dated 30 August 2026. These rules define the review boundary; completed retrieval, screening, and validation require separate reporting of their procedures and results.

\subsection{Review Scope and Eligibility}
\plan{Define the review unit and eligible relationships among life-science context, interactive visualization, and computational assistance. Insert exact E1--E7 criteria, exclusions, and treatment of general frameworks. Apply common eligibility across sources, assistance modes, and modalities. Distinguish eligibility from synthesis priority.}

The screening unit is a paper. When a paper describes multiple systems, at least one identified system must jointly satisfy all seven eligibility criteria below. Evidence for unrelated systems within a paper cannot be combined to establish eligibility. Qualifying systems and their computational mechanisms provide the units for subsequent comparison, but individual mechanisms are not counted as separate corpus members.

E1—Life-science application. The reported system must be applied to analysis, understanding, monitoring, decision-making, discovery, or scientific practice in a life-science or closely related domain. This includes biomedical and clinical science, health, neuroscience, ecology, marine science, biology, and bioinformatics. Clinical and surgical applications follow the same rules as other applications. A surgical system may qualify through an interactive analytic workflow; training, teleoperation, procedural guidance, or presentation alone does not establish that relationship.

E2—Relational, derived-structure, or multiscale relevance. The system must analyze explicit networks or relationships, or relationships derived from spatial, temporal, multivariate, image-derived, lineage, similarity, or other multiscale life-science data. Eligibility therefore does not require a node–link representation. A visualization of derived similarities or spatial relationships may qualify when those relationships have an analytic role.

E3—Interactive visual-analytics component. Humans must use an interactive or analytically meaningful visual representation to inspect, understand, validate, steer, compare, interpret, or act on data or computational results. Visual material used solely for static illustration, presentation, communication, or artistic output does not qualify. A mention of visualization without sufficient description of its analytic role leaves this criterion unresolved.

E4—Substantive computational assistance. At least one computational mechanism must contribute nontrivially within the interactive visual-analytics workflow. It may transform or organize the analytic substrate, adapt interaction, recommend or initiate an action, mediate an explanation, or exploit properties of an immersive environment as assistance. Examples include clustering, dimensionality reduction, graph computation, layout, aggregation, annotation, ranking, prediction, natural-language analytic operations, and workflow orchestration. Ordinary rendering, pan, zoom, click, literal manual selection, direct parameter entry, and ordinary database lookup alone do not qualify. Terms such as “AI,” “adaptive,” or “agentic” are insufficient without evidence of the mechanism’s behavior and analytic role.

E5—Human analytic relationship. Humans must meaningfully inspect, direct, validate, interpret, collaborate with, supervise, correct, or make decisions from the assisted computational process. Fully offline or automated computation without a meaningful human analytic relationship to its results is excluded. The criterion concerns the human role in analysis, rather than requiring manual control over every operation.

E6—Administrative scope. Eligible document types are journal, conference, workshop, and preprint research papers with English-language full text available for review. There is no lower publication-date cutoff; the upper boundary is the documented retrieval end date of the regenerated search run. A survey or conceptual paper qualifies only when it independently reports or analyzes a system satisfying E1–E5. Publication community is not an additional eligibility restriction.

E7—Evidence sufficiency. The reviewed evidence must identify at least one candidate system and support a defensible determination for E1–E6. Insufficient title, abstract, or metadata evidence leaves a record uncertain and requires full-text escalation. Insufficient evidence becomes an exclusion only after documented escalation and human review where required. Retrieval or parsing failure alone does not establish ineligibility.

Each criterion receives a Yes, No, or Uncertain decision supported by the reviewed evidence. A paper is eligible when all seven criteria are Yes, excluded when at least one is conclusively No, and uncertain when no criterion is No but at least one remains unresolved. The same rules apply across retrieval sources, assistance modes, tasks, and visualization environments. Prior corpus membership and historical relevance scores do not determine current eligibility.

Eligibility is also distinct from the priority assigned during synthesis. Core papers provide central evidence about the qualifying system and assistance relationship; Supporting papers materially inform a synthesis question but offer narrower or more limited comparative evidence; Contextual papers primarily supply in-scope historical, framing, or evaluation context. All three priorities refer to eligible papers. General frameworks, surveys, and transferable studies that do not satisfy the criteria may inform the review as background or seed evidence, but remain outside the formal corpus and its counts.

\subsection{Sources, Search Strategy, and Retrieval}
\plan{Document databases, seed sets, citation searching, and the five query families using their executed definitions. Explain complementary coverage and source-specific limitations. Provide full queries and retrieval provenance in supplementary material. Final source counts, seed closure, and global cutoff await completed retrieval.}

The search strategy combines bibliographic searches with candidates identified through prior surveys. These routes address differences in disciplinary coverage and terminology: a biological analysis system may be described through its scientific application, its computational methods, its interface, or its display environment. Retrieval therefore uses several complementary formulations, while eligibility is determined by the common criteria in Section 2.1.

The frozen external-search plan covers PubMed, Europe PMC, Semantic Scholar, arXiv, IEEE Xplore, and the ACM Digital Library. PubMed and Europe PMC provide routes into biomedical and life-science literature; Semantic Scholar broadens identification across disciplines; arXiv provides a preprint source; and direct IEEE Xplore and ACM searches address computing, visualization, and interaction publications. These sources overlap, so a record retrieved through several routes is not evidence of several independent studies. Source occurrences remain distinguishable during the reconciliation described in Section 2.3.

Five query families express different routes into the review’s scope. Each combines life-science terms with the concepts summarized below. They are retrieval strategies, not mutually exclusive categories of papers or substitutes for the eligibility criteria.

\draftnote{Family - Conceptual structure and retrieval purpose}

QF01: Relational visualization - Life-science terms combined with visualization and relational or multiscale terms. This route does not require explicit assistance terminology.

QF02: Assisted visual analysis - Life-science terms combined with visualization or interactive-system terms and assistance terms. It targets computational participation within an analytic interface.

QF03: Interactive systems - Life-science terms combined with interactive-system or specific visual-analytics terms and the revised relational expression. It accommodates descriptions centered on tools, platforms, explorers, or workbenches.

QF04: Nondesktop environments - Life-science terms combined with environment terms and visualization, interactive-system, or exploration terms. Display terminology identifies candidates but does not establish qualifying assistance.

QF05: Conversational analysis - Life-science terms combined with conversational terms and visualization, interactive-system, or analysis terms. Eligibility still requires the visual and human analytic relationships specified in Section 2.1.

The families deliberately overlap. In particular, the assistance-neutral routes allow retrieval of systems whose computational contribution is described in the paper but not advertised through AI terminology. Conversely, matching assistance or environment vocabulary does not establish relevance: a retrieved paper may describe offline prediction, presentation, or procedural guidance without a qualifying visual-analytic workflow. The search broadens the candidate pool; evidence-based screening establishes the boundary.

Provider-specific query syntax and retrieval behavior require separate documentation. A shared conceptual expression does not guarantee identical field coverage, phrase matching, or result sets across services. The retrieval implementation supports paginated API acquisition and import of ACM search exports, with source-specific checkpoints and provenance. Export partitions, interrupted requests, quota pauses, and changes in provider-reported totals must be reconciled before a source is treated as complete. Local metadata-snapshot searches also require their own field and matching definitions: the arXiv snapshot audit reproduced the declared local matching policy but did not establish equivalence to the API. Any snapshot route used in the final corpus must therefore be identified separately, with its temporal coverage and matching limitations.

Prior-survey evidence comprises materials associated with Ehlers et al.’s biological-network visualization survey, Joos et al.’s immersive visual-network analysis survey, and Fonnet and Prié’s immersive-analytics survey. Their available evidence differs: an author companion can identify survey members explicitly, whereas a working ledger or bibliography may contain candidates, background references, and unresolved entries. The prepared seed records preserve these distinctions and their source locations. Inclusion in a prior survey does not confer eligibility for this STAR; candidates remain subject to the same reconciliation and screening rules as database results. Extracting references from these materials also does not establish that recursive backward or forward citation searching was performed.

The supplementary search record will provide the full executed expressions, searched fields, retrieval dates, filters, source routes, export or snapshot identifiers, and completion evidence. It will also identify the seed artifacts and any additional citation-searching procedure actually used. Final source yields, unique contributions, seed reconciliation, and the global retrieval cutoff remain pending completion of the merged identification record.

\draftnote{Author notes — remove before submission:

This is a provisional methods draft grounded in the frozen protocol and available development/execution reports, not a claim that all six routes have completed. Full source-native production query strings were not available in the inspected artifacts; the table is a conceptual summary, not a copyable query specification.

Verify the final production plan and executed manifests before changing “plan covers” to “searched.” In particular, confirm the final Semantic Scholar and arXiv routes and any subsequent matching-policy revisions.

Resolve actual backward/forward citation-search scope, seed selection, iteration/stopping rules and yields; none is claimed here. Seed extraction is distinguished from recursive citation chasing.

Insert live citations for the three surveys during LaTeX integration. FP19 is an internal seed ID, not a reliable publication year; its historical bibliographic conflict requires the corrected record.

Move finalized route details into the supplement and replace the last paragraph with completed-report wording. No final counts, cutoff, seed closure, recall estimate, or screening/validation result is asserted.}

\subsection{Occurrence Preservation and Deduplication}
\plan{Explain how source occurrences remain traceable when records merge into unique works. Specify normalization, matching, version handling, and uncertain-match adjudication from the implementation. Preserve source-to-paper lineage for reconciled flow counts.}

A paper may be retrieved from several databases, match several query families, and appear in a prior survey. These are separate source occurrences of potentially the same paper. The review pipeline preserves each occurrence and its provenance while grouping records that refer to the same bibliographic identity. This allows the synthesis to count a paper once without losing the evidence of how it was identified.

Each occurrence remains linked to its source record and retrieval context. For database results, this includes the source, query, retrieval run, and available native identifiers. Imported survey and export records retain their artifact and entry locations, such as a workbook row, companion-resource identifier, or bibliography entry. A canonical record represents the resulting identity group and enumerates its constituent occurrences. The data model requires each occurrence to have one effective duplicate decision and to belong to one canonical group. Source-specific metadata remain available through those occurrences when records disagree.

The generic matching rule uses a normalized DOI when one is available and an exact normalized-title key otherwise. This is a deterministic key-based procedure: title fallback does not imply a search for the most similar title. It also does not automatically join a DOI-bearing record to a DOI-missing record with the same title, because those records receive different keys. Such cases illustrate why the initial grouping and subsequent identity adjudication must remain distinguishable. The normalization functions and matching-rule versions belong in the reproducibility record so that the same source metadata can be processed under the same rules.

Approved identity adjudications supplement the generic grouping. The snapshot-integration procedure distinguishes a direct decision about an arXiv occurrence from the propagation of that decision to compatible occurrences already grouped under a title key. For example, a title-only survey record and a snapshot record may share an initial group, while another database supplies the DOI-bearing record for the same paper. An approved identity decision can consolidate that group with the DOI-bearing target while preserving all constituent occurrences. The implementation checks for contradictory identifiers, competing targets, unresolved identity restrictions, and cyclic transfers before applying a propagated regrouping. Earlier decisions remain in the history, linked to the decision that supersedes them.

Bibliographic identity is distinct from a relationship between versions. A preprint and a published article may be related without being interchangeable records. The implementation therefore retains related-version links separately from duplicate decisions; those links alone do not collapse the records into one identity. This distinction also matters during synthesis: multiple reports of the same system should not be interpreted as independent evidence merely because they remain separate bibliographic records. Any subsequent grouping of reports for comparison must be documented separately from record deduplication.

Uncertain identities remain visible. In particular, provisional groups inherited from prior-survey materials retain their unresolved status and source-group references through the merge. A shared title or membership in a source ledger does not erase conflicting bibliographic evidence. Identity uncertainty is also separate from uncertainty about eligibility: a paper may be identifiable but insufficiently described for screening, or potentially relevant while its relationship to another record remains unresolved. Neither condition is resolved simply by placing the record in a canonical group.

The merge procedure selects the designated source datasets and checks preservation of their occurrences and retrieval provenance. Historical checkpoints and superseded artifacts remain part of the acquisition history but are not added again as independent inputs. Review-flow reporting must distinguish the number of source occurrences, the number consolidated as duplicates, and the number of resulting canonical records, with unresolved identities reported alongside them. Deduplication is a reconciliation step rather than an eligibility exclusion. Final counts will be derived from the completed merge and its effective decisions, preserving a traceable path from each source occurrence to the paper considered during screening.

\draftnote{Author notes — remove before submission:

This describes documented implementation behavior, not a completed global merge. No final canonical counts, resolved-case totals or completed screening are claimed.

Verify normalize.py at the final bound implementation commit and insert the exact DOI/title transformations (prefix stripping, case, Unicode, punctuation and whitespace behavior as actually implemented). The inspected reports establish DOI-first/exact-normalized-title fallback, but do not specify every transformation. Do not borrow tokenization rules from the arXiv query matcher: that is a different operation.

Verify how records missing both DOI and title are keyed and retained, and how canonical display metadata are selected in the final implementation. Neither behavior is inferred here.

Finalize and report the identity-review actor/procedure and handling of unresolved cases before claiming complete adjudication. The documented snapshot approvals do not establish a general independent double-review procedure.

Related-version links currently preserve separate bibliographic records. Define any later report/system grouping and its effect on synthesis denominators; do not silently equate canonical papers with independent studies.

Sources inspected: production-core audit 20260901-032214 (historical core invariants only); executed provisional canonicalization report 20260904-034804 (matching behavior, not production counts); global-merge implementation/preflight report 20260920-052012; canonical-group propagation fix report 20260920-171039 (commit 74ac671). Later reports supersede historical claims that adjudication orchestration was absent.}

\subsection{Screening, Full-Text Escalation, and Human Decisions}
\plan{Describe title/abstract screening, evidence requirements, full-text escalation, and exclusion reasons. Identify what the LLM proposes and what humans decide. Report actual validation sampling, reviewers, and disagreement handling once performed. Do not imply independent double screening unless it occurred.}

Screening applies the eligibility criteria in Section 2.1 to the evidence available for each paper. Title and abstract assessment provides an initial determination of whether the reported system falls within scope and which questions require fuller examination. Each criterion receives a Yes, No, or Uncertain value, supported by an evidence location and a concise rationale. A general claim that a system is intelligent, interactive, or immersive is insufficient to establish the computational mechanism or the human analytic relationship. Where a paper describes several systems, the evidence must identify which system satisfies the criteria.

The protocol assigns LLMs a supporting role in this process. Their outputs are proposed criterion-level decisions whose basis must remain inspectable. The record preserves the reviewed input, rubric version, model and prompt provenance, proposed values, supporting evidence, rationale, and technical status. These proposals do not themselves establish authoritative corpus membership. Administrative criteria also require evidence about publication date, document type, and English-language full-text availability; they cannot be inferred solely from a model’s assessment of topical relevance. A malformed response or failed inference attempt is recorded as a technical failure and cannot become a scientific exclusion.

Full-text escalation is required when any eligibility criterion remains uncertain, a proposed exclusion rests on a missing or incomplete abstract, or available sources conflict. It is also required when the available description cannot distinguish substantive assistance from ordinary rendering or interaction, immersive assistance from use of an immersive display, or an analytic workflow from presentation or procedural guidance. This step addresses a limitation of abstract screening: an omitted detail may be essential to eligibility without being central to the paper’s summary. The fuller assessment must resolve the relevant criterion through evidence rather than treat the omission itself as a negative result.

Acquisition attempts and their outcomes form part of the screening record. Retrieval or parsing failure leaves the scientific decision unresolved while further acquisition or review remains possible. After reasonable documented attempts, unresolved records proceed to human review. Exclusion for insufficient evidence is permitted only after the required escalation has been completed and recorded. Confirmed administrative ineligibility, such as the absence of an English-language full text, is recorded under its corresponding criterion rather than conflated with a failed download.

Exclusions retain one primary reason and any supported secondary reasons. The rubric gives administrative reasons precedence—publication after the retrieval end date, unavailable English-language full text, and ineligible document type—followed by absence of a life-science application, relational or multiscale relevance, a qualifying visual-analytics component, substantive assistance, or a human analytic relationship. Insufficient evidence after escalation is a separate reason. Any other protocol-based exclusion requires a versioned explanation. Duplicate records are handled through the reconciliation in Section 2.3, while missing abstracts, technical failures, and historical relevance labels are not exclusion reasons.

Human reviewers use the same criterion structure and identify the evidence supporting their decisions. Under the protocol, a human decision supersedes the corresponding machine proposal for the criterion reviewed; the earlier proposal remains in the history. Where independent human judgments disagree, a recorded adjudication links those judgments and explains the resolution. This provision defines how disagreements are handled when they arise; it does not imply that every paper receives independent double screening. Eligibility decisions remain separate from the multilabel coding and synthesis priorities described in Section 2.5, and authors retain responsibility for the interpretive synthesis.

The frozen protocol leaves the final human-validation design to be registered prospectively after a bounded pilot and before validation results are examined. That design must specify the sample frame, selection procedure, reviewer roles, access to machine proposals, adjudication, missing-review handling, and planned measures. The completed review must report the procedures actually used, the resulting agreement and error evidence, and any corrections. Those details remain pending here; implementation checks and pilot demonstrations do not establish the validity of screening across the regenerated corpus.

Author notes — remove before submission:

Grounded in the frozen protocol and coding rubric v1.0.0 (30 August 2026), already read in full. This is a protocol-grounded draft, not evidence that production screening or validation has occurred.

Insert the executed title/abstract and full-text stages, actual model/provider/version, prompt/schema versions, input fields, inference settings, evidence-validation behavior and retry policy once verified against production artifacts. Do not substitute details from the historical provisional pilot.

Report who made authoritative decisions, which criteria/records they reviewed, and whether they saw machine proposals. Do not claim all machine proposals received human review or independent double screening unless documented.

Replace the final paragraph with the registered sample design and actual validation results, including selection probabilities or census rules, strata, random seed where used, coder independence, disagreement handling and missing reviews. A previously proposed sample size is not an executed design.

Specify actual full-text acquisition routes and the operational stopping rule for reasonable attempts. Distinguish unresolved acquisition from established language ineligibility; do not automatically label inaccessible PDFs non-English.

Ensure supporting passages resolve to the actual reviewed source text. The manuscript describes evidence requirements; final automated span checks require implementation verification.

Final exclusion totals and reconciled flow belong in 2.6. No screening totals or corpus-wide accuracy estimates are supplied here.

\subsection{Multilabel Coding and Synthesis}
\plan{Define task, assistance, and modality coding with supporting evidence passages. Explain Core/Supporting/Contextual priorities without conflating them with inclusion. Specify the comparison unit for systems with multiple mechanisms. Interpret agency from documented interventions and human roles rather than adding another categorical score.}
\subsection{Review Flow, Reproducibility, and Limitations}
\plan{Insert the reconciled PRISMA flow, validation results, and availability statement for queries, decisions, coding, and provenance. Discuss coverage, unavailable full texts, LLM errors, and reviewer judgment. Separate review-process limitations from limitations of included studies. Do not reuse the submitted corpus totals.}
